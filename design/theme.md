# claufy — theme (blank slate)

Readable copy of this project's look. The executable copy is `design/image-pipeline.json`.

- Locked style block: (paste into graph node 6 `text`)
- Locked seed: (graph node 25 `noise_seed`)
- Working aspect: (graph node 27 `width` x `height`)
- Sampler / scheduler / steps: (graph nodes 16 / 17)
- Model: madroid/flux.1-schnell-mflux-4bit (see image-pipeline/README.md)
